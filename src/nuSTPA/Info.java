package nuSTPA;

import java.util.ArrayList;

import nuSTPA.model.step2.Controller;

public class Info {

	public static String directory = System.getProperty("os.name").toLowerCase().contains("win") ? 
			"C:/" : "/Users/";
}
